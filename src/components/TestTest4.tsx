import { FC } from "react";
import { View } from "react-native";
import { Text } from "react-native-paper";

type TestTest4Props = {};

const TestTest4: FC<TestTest4Props> = (props) => {
  return (
    <View>
      <Text>TestTest4</Text>
    </View>
  );
};

export default TestTest4;
