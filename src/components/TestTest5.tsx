import { FC } from "react";
import { View } from "react-native";
import { Text } from "react-native-paper";

type TestTest5Props = {};

const TestTest5: FC<TestTest5Props> = (props) => {
  return (
    <View>
      <Text>TestTest5</Text>
    </View>
  );
};

export default TestTest5;
