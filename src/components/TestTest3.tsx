import { FC } from "react";
import { View } from "react-native";
import { Text } from "react-native-paper";

type TestTest3Props = {};

const TestTest3: FC<TestTest3Props> = (props) => {
  return (
    <View>
      <Text>TestTest3</Text>
    </View>
  );
};

export default TestTest3;
