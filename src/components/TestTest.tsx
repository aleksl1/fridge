import { FC } from "react";
import { View } from "react-native";
import { Text } from "react-native-paper";

type TestTestProps = {};

const TestTest: FC<TestTestProps> = (props) => {
  return (
    <View>
      <Text>TestTest</Text>
    </View>
  );
};

export default TestTest;
